{
  "name": "hsk-app-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling cho app học HSK (app chạy không cần dependency; đây chỉ là công cụ build dữ liệu).",
  "type": "module",
  "scripts": {
    "build:words": "node scripts/build-words.mjs"
  },
  "devDependencies": {
    "opencc-js": "^1.0.5",
    "xlsx": "^0.18.5"
  }
}
